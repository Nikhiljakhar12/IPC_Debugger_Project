version: '3.8'
services:
backend:
build: ./backend
ports:
- "4000:4000"
volumes:
- ./backend:/app
frontend:
build:
context: ./frontend
command: ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0 --port 5173"]
ports:
- "5173:5173"
volumes:
- ./frontend:/app
depends_on:
- backend
